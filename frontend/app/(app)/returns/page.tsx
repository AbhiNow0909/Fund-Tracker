import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function ReturnsPage() {
  return <PagePlaceholder title="Returns Analyzer" subtitle="Trailing & rolling returns vs benchmark" step="Step 13" />;
}
