import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function TaxPage() {
  return <PagePlaceholder title="Tax / Capital Gains" subtitle="Realised gains, harvesting & estimated tax" step="Step 15" />;
}
