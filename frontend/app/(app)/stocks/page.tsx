import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function StocksPage() {
  return <PagePlaceholder title="Stocks / Shares" subtitle="Direct equity held in your demat account" step="Step 9" />;
}
